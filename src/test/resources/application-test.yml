spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/salon_test?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: salon
    password: salon
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: false

logging:
  level:
    com.salon: INFO
